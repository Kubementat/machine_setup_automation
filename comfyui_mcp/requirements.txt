# Installed by tasks/setup-comfyui.sh into ${COMFYUI_MCP_DIR}/.venv.
# Pinned on purpose: the server parses comfy-cli's JSON envelope (envelope/1)
# and uses the mcp 2.x MCPServer API.
mcp==2.2.0
comfy-cli==1.20.0
