# Managed by tasks/setup-comfyui.sh — re-run the task instead of editing this.
#
# COMFYUI_MCP=true: gives llama-swap the ComfyUI MCP server's own API key,
# which the 51-comfyui-mcp.yaml fragment appends to apiKeys. Every llama-swap
# child inherits it; comfyui-launch unsets it before ComfyUI starts.
[Service]
EnvironmentFile=${COMFYUI_MCP_DIR}/.env
